# Repository import completed.
name: CDP import archive

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The feature/cdp-v3 repository import is complete."
